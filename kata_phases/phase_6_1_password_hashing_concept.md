# Phase 6.1: 密碼雜湊概念理解

## 🎯 本Phase學習目標
- 理解為什麼絕對不能明碼儲存密碼
- 學習雜湊(Hash)vs加密(Encryption)的差異
- 了解bcrypt和salt的作用
- 建立安全意識的基礎

---

## 📋 具體任務描述
閱讀和理解密碼安全的基本概念，實作簡單的密碼雜湊測試程式。

## 🔧 實作要求

### ✅ 必須完成的項目
1. **安裝bcrypt**: `pip install bcrypt`
2. **建立測試程式**: 體驗密碼雜湊過程
3. **理解salt概念**: 觀察同樣密碼的不同雜湊值
4. **驗證過程**: 理解雜湊值如何驗證密碼

### 📝 測試程式範例
```python
# 建立 password_test.py 來實驗
import bcrypt

def test_password_hashing():
    # 原始密碼
    password = "mySecretPassword123"

    # 生成雜湊
    hashed1 = bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt())
    hashed2 = bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt())

    print(f"原始密碼: {password}")
    print(f"雜湊值1: {hashed1}")
    print(f"雜湊值2: {hashed2}")
    print(f"雜湊值相同嗎? {hashed1 == hashed2}")

    # 驗證密碼
    is_valid1 = bcrypt.checkpw(password.encode('utf-8'), hashed1)
    is_valid2 = bcrypt.checkpw("wrongPassword".encode('utf-8'), hashed1)

    print(f"正確密碼驗證: {is_valid1}")
    print(f"錯誤密碼驗證: {is_valid2}")

if __name__ == "__main__":
    test_password_hashing()
```

---

## 📚 學習重點

### 🔍 核心概念
1. **雜湊 vs 加密**:
   - 雜湊: 單向函數，無法復原
   - 加密: 雙向函數，可以解密

2. **Salt的作用**:
   - 防止彩虹表攻擊
   - 確保相同密碼有不同雜湊值

3. **bcrypt的優勢**:
   - 適應性強(可調整複雜度)
   - 內建salt機制
   - 抗暴力破解

### 🚨 安全原則
- 永遠不儲存明碼密碼
- 使用強大的雜湊演算法
- 每個密碼都要有唯一的salt
- 定期檢討密碼強度政策

---

## 🔍 實驗任務

### 🧪 觀察實驗
1. **相同密碼不同雜湊**:
   ```python
   # 觀察同樣的密碼每次雜湊後的結果
   for i in range(3):
       hashed = bcrypt.hashpw("password123".encode('utf-8'), bcrypt.gensalt())
       print(f"第{i+1}次: {hashed}")
   ```

2. **驗證實驗**:
   ```python
   # 測試正確和錯誤密碼的驗證結果
   hashed = bcrypt.hashpw("correct".encode('utf-8'), bcrypt.gensalt())
   print(bcrypt.checkpw("correct".encode('utf-8'), hashed))    # True
   print(bcrypt.checkpw("wrong".encode('utf-8'), hashed))      # False
   ```

3. **成本實驗**:
   ```python
   import time
   # 比較不同cost的計算時間
   for cost in [10, 12, 14]:
       start = time.time()
       bcrypt.hashpw("test".encode('utf-8'), bcrypt.gensalt(rounds=cost))
       end = time.time()
       print(f"Cost {cost}: {end-start:.3f}秒")
   ```

---

## 📖 學習筆記區

### 🤔 我的理解
```
1. 為什麼不能儲存明碼密碼：


2. 雜湊和加密的差異：


3. Salt的重要性：


4. bcrypt的優勢：

```

### 🔬 實驗觀察
```
相同密碼的雜湊值觀察：

不同cost的時間差異：

意外發現：
```

### 💡 安全思考
```
在現實專案中的應用：

還需要考慮什麼安全因素：
```

---

## 🚀 下一步預告
**Phase 6.2** 將實作密碼雜湊和驗證的utility函數，整合到我們的專案中。

---

## ⏰ 執行記錄
- **開始時間**: ___________
- **完成時間**: ___________
- **最深刻的學習**: ___________