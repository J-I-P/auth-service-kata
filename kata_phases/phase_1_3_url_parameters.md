# Phase 1.3: 路徑參數和查詢參數

## 🎯 本Phase學習目標
- 理解路徑參數(path parameters)的使用
- 學會處理查詢參數(query parameters)
- 實作動態路由
- 理解參數型別轉換

---

## 📋 具體任務描述
實作兩個新endpoints來展示不同類型的參數處理：路徑參數和查詢參數。

## 🔧 實作要求

### ✅ 必須完成的項目
1. **GET /user/{user_id}**: 使用路徑參數取得使用者ID
2. **GET /search**: 使用查詢參數進行搜尋功能
3. **型別轉換**: 正確處理整數和字串參數
4. **參數驗證**: 基本的參數檢查

### 📝 實作結構提示
```python
# 路徑參數範例
@app.get("/user/{user_id}")
async def get_user(user_id: int):
    return {"user_id": user_id, "message": f"Getting user {user_id}"}

# 查詢參數範例
@app.get("/search")
async def search(q: str, limit: int = 10):
    return {
        "query": q,
        "limit": limit,
        "results": f"Searching for '{q}' with limit {limit}"
    }
```

---

## 📚 學習重點

### 🔍 核心概念
1. **路徑參數**: URL路徑中的變數部分
2. **查詢參數**: URL中?後面的key=value對
3. **型別轉換**: FastAPI自動轉換字串為指定型別
4. **預設值**: 查詢參數的預設值處理

---

## 📖 學習筆記區

### 🤔 我的理解
```
路徑參數 vs 查詢參數的使用時機：

型別轉換的好處：
```

---

## ⏰ 執行記錄
- **開始時間**: ___________
- **完成時間**: ___________